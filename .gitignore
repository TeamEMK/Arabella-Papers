node_modules/
.env
*.log
.DS_Store
uploads/
.vercel

# Live database connection string for scripts/backup-live.js
.env.backup
credentials.json

# Order snapshots taken before a repair — live data, kept to hand, not in the repo.
*-before-repair.json
